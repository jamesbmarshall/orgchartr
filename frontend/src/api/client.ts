// Data access goes through the active storage adapter (server HTTP API or browser
// local-folder mode). This module keeps the historical import path for stores/components.
export { api, photoUrl } from '../storage/active';
