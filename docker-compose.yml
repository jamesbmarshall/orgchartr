services:
  app:
    build:
      context: .
      dockerfile: Dockerfile
    ports:
      - '3000:3000'
    volumes:
      - type: bind
        source: ${ORGCHARTR_DATA_DIR:-./data}
        target: /app/data
    restart: unless-stopped
