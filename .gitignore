node_modules/
dist/
build/
*.log
.DS_Store
.env
.env.local

# Data is deliberately NOT part of this repo - it lives in its own local git repo
# (data/.git) so partner/sponsor data never ends up in the app-code remote.
# See README.md "Data storage" section.
data/

# Editor
.vscode/*
!.vscode/extensions.json
