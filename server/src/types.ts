// Persisted-data types live in the shared workspace (single source of truth for
// server, frontend, and the browser local-folder adapter).
export type {
  Chart,
  ChartHistoryEntry,
  ChartIndexEntry,
  Person,
  Position,
  Sponsor,
  SponsorUsageEntry,
} from '@orgchartr/shared';
